// import { useState } from 'react'
import './Carousel.scss'

function Carousel({ house }) {
  // const [data, setData] = useState([])

  return (
    <div className="carousel">
    </div>
  )
}

export default Carousel
